# 开发日志 — 2026-03-02

## 摘要

Phase 2 核心功能大规模实现 + 国际化扩展 + App Store 合规准备。上午完成证件照规格体系、Pro 订阅门控、自定义尺寸、排版打印、美颜/服装选项、前后对比滑块、4 语言支持、iPad 适配、iOS 26 Liquid Glass UI 全面升级。下午扩展多语言至 7 语言（新增越南语/印尼语/巴西葡萄牙语），创建 7 语言×2 的法律文档（隐私政策 & 服务条款），完善 Gemini 后端 prompt 安全，优化订阅定价方案。

---

## 完成任务

### 1. 证件照规格体系 (`IDPhotoSpec`)

- 新建 `Models/IDPhotoSpec.swift`，定义 10+ 证件照规格（enum + CaseIterable + Identifiable）
- 覆盖：日本パスポート/マイナンバー/履歴書/在留カード、中国一寸/二寸/护照、韩国여권/이력서、美国 Passport
- 每个规格含：尺寸（mm）、背景色、Pro 标记、图标、4 语言显示名、AI prompt
- 地区分组排序：根据当前语言动态排列（本地区优先）

### 2. Pro 订阅门控

- `SpecSelectorView` 新增 `isSubscribed` / `onLockedTap` 参数
- Pro 规格卡片显示锁定图标（橙色渐变圆形 lock.fill）
- 免费用户点击 Pro 规格 → 触发订阅弹窗
- `SubscriptionSheetView` 全面重构：功能列表、价格展示、合规链接

### 3. Pro 自定义尺寸

- 新增 `CustomSizeSpec` 模型（宽 20~60mm，高 20~80mm，默认 35×45mm）
- `SpecSelectorView` 末尾添加「自定义」卡片（PRO 角标）
- 新建 `CustomSizePickerView`：Stepper 控件调节宽高，玻璃效果卡片
- `ContentView` 集成：`isCustomSize` 状态驱动 prompt 和 UI 切换

### 4. 排版打印功能

- 新建 `Models/PrintLayout.swift`：`PrintPaperSize`（L判/2L判）+ `PrintLayoutInfo` 网格计算
- 新建 `Services/PrintLayoutService.swift`：300 DPI 渲染引擎，裁切线绘制
- 新建 `Views/PrintLayoutSheetView.swift`：纸张选择、实时预览、保存到相册
- 支持预设规格和自定义尺寸（`photoSizeMM` tuple 接口）

### 5. 美颜 & 服装选项

- 新建 `Models/PhotoOptions.swift`：`BeautyLevel`（自然/清透/精致）、`OutfitStyle`（正装/商务/学生等）
- 新建 `Views/Components/ProOptionsView.swift`：可折叠选项面板，Pro 门控
- prompt 动态拼接美颜和服装描述

### 6. 前后对比滑块

- 新建 `Views/Components/ComparisonSliderView.swift`：拖拽滑块对比原图与生成结果
- 圆形拖拽手柄 + 「前/后」标签

### 7. 多语言支持

- 新建 `Managers/LanguageManager.swift`：4 语言切换（中/英/日/韩）
- 新建 `Resources/` 下 4 个 `.lproj/Localizable.strings`
- 新建 `Views/SettingsView.swift`：语言切换、隐私政策、版本号

### 8. iPad 适配

- 移除双栏 `iPadLayout`，统一为单栏 `mainLayout`
- 通过 `maxWidth: 600` 实现 iPad 居中布局

### 9. iOS 26 Liquid Glass UI

- `GlassBackground` 升级 `.glassEffect()` 修饰符
- 所有卡片组件采用 Liquid Glass 风格
- `SpecCard` 选中态：`.glassEffect(.regular.tint(.blue))`
- `scrollClipDisabled(true)` 解决 ScrollView 裁切 glass 阴影问题

### 10. StoreKit 2 配置

- 新建 `Configuration/Products.storekit`：StoreKit 测试配置
- `SubscriptionManager` 重构：Product ID、entitlement 检查逻辑

### 11. 自定义字体

- 引入 Plus Jakarta Sans（Bold / SemiBold）用于标题展示

---

## 技术决策

| 决策 | 选择 | 原因 |
|------|------|------|
| 规格数据结构 | Swift enum + CaseIterable | 类型安全、编译期检查、switch 穷举 |
| 自定义尺寸范围 | 宽 20~60mm、高 20~80mm | 覆盖常见证件照尺寸，避免无意义极端值 |
| 排版打印 DPI | 300 DPI | 便利店打印标准（L判/2L判） |
| iPad 布局 | 单栏 + maxWidth 600 | 简单可靠，避免双栏在不同 iPad 尺寸上的适配问题 |
| 多语言实现 | 手动 switch + LanguageManager | 灵活控制，不依赖系统语言设置 |
| Liquid Glass 阴影 | scrollClipDisabled(true) | iOS 26 内置阴影无法被 .shadow(color: .clear) 覆盖 |

---

## 问题 & 解决方案

| 问题 | 原因 | 解决 |
|------|------|------|
| iPad 双栏布局拥挤 | 内容不足以填充两栏 | 移除双栏，统一单栏 + maxWidth |
| SpecCard 阴影被 ScrollView 截断 | ScrollView 默认 clip | `.scrollClipDisabled(true)` |
| Liquid Glass 阴影无法覆盖 | `.shadow(color: .clear)` 无效 | 改为给阴影更多空间 + scrollClipDisabled |
| Edit tool 匹配重复字符串 | 两处 SpecSelectorView 调用代码完全相同 | 扩大上下文范围使匹配唯一 |

---

## 新增文件清单

```
ios/AIIDPhoto/
├── Configuration/Products.storekit
├── Managers/LanguageManager.swift
├── Models/
│   ├── IDPhotoSpec.swift
│   ├── PhotoOptions.swift
│   └── PrintLayout.swift
├── Resources/
│   ├── Fonts/PlusJakartaSans-{Bold,SemiBold}.ttf
│   ├── en.lproj/Localizable.strings
│   ├── ja.lproj/Localizable.strings
│   ├── ko.lproj/Localizable.strings
│   └── zh-Hans.lproj/Localizable.strings
├── Services/PrintLayoutService.swift
├── Views/
│   ├── PrintLayoutSheetView.swift
│   ├── SettingsView.swift
│   └── Components/
│       ├── ComparisonSliderView.swift
│       ├── CustomSizePickerView.swift
│       ├── ProOptionsView.swift
│       └── SpecSelectorView.swift
```

### 12. 多语言扩展（+3 语言）

新增越南语 (vi)、印尼语 (id)、巴西葡萄牙语 (pt) 支持，全 app 从 4 语言扩展到 7 语言。

**LanguageManager 更新：**
- 新增 `.vietnamese`、`.indonesian`、`.portuguese` 枚举值
- `effectiveCode` 自动检测系统语言（vi / id,ms / pt 前缀匹配）
- 国旗 emoji：🇻🇳 / 🇮🇩 / 🇧🇷

**UI 翻译覆盖（300+ 字符串）：**
- `ContentView` — 35+ 字符串（标题、按钮、生成进度消息、广告提示）
- `SettingsView` — 全部 section 标题、剩余次数、到期日、3 个新语言行
- `SubscriptionSheetView` — Hero、6 大权益（标题+描述）、计划、CTA、社会证明、合规
- `SpecSelectorView` — 规格标题、自定义标签
- `CustomSizePickerView` — 标题、宽/高标签、范围提示
- `ProOptionsView` — 6 大选项分类标题
- `PrintLayoutSheetView` — 10+ 字符串、4 步操作说明
- `IDPhotoSpec` — 15 个规格显示名
- `PhotoOptions` — 5 个枚举共 17 个选项显示名
- `PrintLayout` — 纸张名称、价格提示

**基础设施：**
- `project.yml` 添加 vi / id / pt-BR lproj 资源路径 + CFBundleLocalizations
- 创建 `Resources/{vi,id,pt-BR}.lproj/Localizable.strings`（空文件，app 使用代码内翻译）

**地区优化：**
- `japanResidenceCard.primaryRegions` 新增 "VN"（在日越南人常用）

### 13. 法律文档（隐私政策 & 服务条款）

为 App Store 上架创建多语言法律页面：

```
docs/legal/
├── index.html              # 语言选择入口（7 语言）
├── privacy/
│   ├── en.html / ja.html / zh.html / ko.html
│   ├── vi.html / id.html / pt.html
└── terms/
    ├── en.html / ja.html / zh.html / ko.html
    ├── vi.html / id.html / pt.html
```

**合规要点：**
- Apple Guideline 5.1.2(i)：明确指名 Google Gemini API 作为第三方 AI 服务
- 面部照片作为生物识别敏感数据的处理说明
- 日本 APPI / 韩国 PIPA / 一般性 GDPR 合规声明
- 数据不存储、HTTPS 加密、GCP 东京区域
- 联系方式：weiwenxin.dev@gmail.com

### 14. 后端 Prompt 安全

- `GeminiService.swift` 新增系统级 prompt 安全约束：禁止注入攻击、强制仅输出人像照片
- 后端 `gemini.ts` 同步加固

### 15. 订阅定价优化

- `Products.storekit` 更新定价：月付 $1.99 → 年付 $12.99
- 移除免费试用期（直接付费转化）
- 收入模型分析：1000 付费用户 × 保守场景（30/70 月年比）→ 月净 ¥172,500

---

## 技术决策（补充）

| 决策 | 选择 | 原因 |
|------|------|------|
| 新增语言翻译方式 | `l()` 函数添加 optional 参数 | 向后兼容，已有 4 语言代码无需改动 |
| 法律文档格式 | 静态 HTML | 可部署到 GitHub Pages 或任意静态托管，无需后端 |
| Prompt 安全 | 系统级指令约束 | 防止用户通过自定义 prompt 生成非证件照内容 |
| 免费试用 | 移除 | 降低羊毛党风险，直接验证付费意愿 |

---

## 问题 & 解决方案（补充）

| 问题 | 原因 | 解决 |
|------|------|------|
| Edit tool 无法修改未读文件 | 上下文压缩后文件内容丢失 | 先 Read 再 Edit |
| iPhone 16 模拟器不存在 | Xcode 26.2 环境 | 改用 iPhone 17 Pro |
| 300+ 翻译字符串效率 | 单线程逐文件太慢 | 5 个并行 Agent 分组处理 |

---

## 新增文件清单（补充）

```
docs/legal/
├── index.html
├── privacy/{en,ja,zh,ko,vi,id,pt}.html   (7 files)
└── terms/{en,ja,zh,ko,vi,id,pt}.html     (7 files)

ios/AIIDPhoto/Resources/
├── vi.lproj/Localizable.strings
├── id.lproj/Localizable.strings
└── pt-BR.lproj/Localizable.strings
```

---

---

## Phase 3 — 产品优化 & 增长引擎（下午）

### 16. 法律文档 URL 修复 + 后端静态托管

- `backend/Dockerfile` 添加 `COPY public/ ./public/` 打包法律文档到 Docker 镜像
- `backend/src/index.ts` 添加 `express.static` 中间件：`/legal/*` 路径提供静态 HTML
- `backend/public/legal/` 从 `docs/legal/` 复制 7 语言 × 2 文档 + index.html
- `SettingsView.swift` 和 `SubscriptionSheetView.swift` 中 `example.com` 占位符 URL 替换为语言感知的真实 URL
- 新增 `LegalURLs` 枚举：根据 `LanguageManager.effectiveCode` 构建 `{backendURL}/legal/privacy/{lang}.html`

### 17. 新用户引导 Onboarding

- 新建 `Views/OnboardingView.swift`：4 页 TabView + PageTabViewStyle
  - Page 1: AI 智能生成（`wand.and.stars`，蓝色）
  - Page 2: 多种规格（`doc.text.image.fill`，紫色）
  - Page 3: 便利店打印（`printer.fill`，青色）
  - Page 4: 开始使用（CTA 按钮 "免费开始"）
- `AIIDPhotoApp.swift` 添加 `@AppStorage("hasSeenOnboarding")`，`.fullScreenCover` 展示引导
- Glass UI 风格、7 语言翻译、跳过按钮

### 18. App Store 评分引导

- `ContentView.swift` 添加 `import StoreKit`
- `@AppStorage("successfulGenerations")` + `@AppStorage("lastReviewPromptVersion")`
- 第 3 次生成成功后、且当前版本未提示过 → 延迟 2s 调用 `AppStore.requestReview(in:)`

### 19. 轻量级 Analytics Manager

- 新建 `Managers/AnalyticsManager.swift`：单例模式，UserDefaults 存储
- 11 个事件：app_open / onboarding_complete / photo_selected / generation_success / generation_failed / paywall_shown / subscription_started / ad_watched / photo_saved / photo_shared / print_used
- 本地 JSON 序列化，最多 500 条自动清理
- DEBUG 模式 `[Analytics]` 控制台输出
- 可选 `flush(to:)` 方法预留后端同步接口
- 在 ContentView 关键路径埋点（生成成功/失败、保存、分享、付费墙、广告观看）

### 20. 社交分享

- `ContentView.swift` resultActions 区域新增分享按钮
- `UIActivityViewController` 实现（支持动态水印，ShareLink 不适合）
- 免费用户：右下角半透明 "AI ID Photo" 水印（`UIGraphicsImageRenderer` 绘制）
- Pro 用户：无水印分享
- 分享文本包含 App Store 下载链接
- iPad 适配 popover（`popoverPresentationController.sourceView`）

### 21. 生成历史

- 新建 `Models/GenerationRecord.swift`：Codable 记录模型（id, date, spec, thumbnail）
- 新建 `Managers/HistoryManager.swift`：Documents/history/ 目录存储
  - 缩略图压缩 JPEG（512px, 0.7 质量）
  - 最多 50 条自动清理（删除最旧记录及缩略图文件）
  - JSON 元数据持久化
- 新建 `Views/HistoryView.swift`：LazyVGrid 画廊 + 上下文菜单（保存/删除）+ 空状态
- `ContentView` 集成：底部工具栏时钟图标、生成成功后自动保存记录

### 22. 推荐邀请机制

**后端：**
- 新建 `backend/src/routes/referral.ts`：
  - `POST /register`：根据 deviceId 哈希生成 6 位邀请码
  - `POST /redeem`：兑换邀请码（防自兑换、防重复兑换）
  - 每次兑换双方各得 3 次 Pro 品质生成
- `backend/src/index.ts` 注册 referral 路由

**iOS：**
- 新建 `Managers/ReferralManager.swift`：注册码、兑换、奖励次数管理
- `SettingsView.swift` 添加"邀请好友"区块：显示邀请码、分享按钮、兑换码输入框
- `ContentView.swift` generateTapped() 中优先消耗推荐奖励次数
- `AIIDPhotoApp.swift` 注入 ReferralManager，首次启动自动注册邀请码

### 23. Cloud Run 部署修复

- 部署失败原因：Compute Engine 默认服务账号缺少 `roles/artifactregistry.writer` 权限
- Docker 镜像构建成功但推送到 Artifact Registry 失败
- 修复：`gcloud projects add-iam-policy-binding` 添加 `artifactregistry.writer` 和 `logging.logWriter` 角色
- 重新部署成功，所有端点验证通过

---

## 技术决策（Phase 3）

| 决策 | 选择 | 原因 |
|------|------|------|
| 法律文档托管 | Cloud Run express.static | 无需额外服务，与后端统一部署 |
| Analytics | 轻量级自建（UserDefaults + JSON） | 无 Firebase SDK 依赖，包体积小，隐私友好 |
| 分享实现 | UIActivityViewController | 支持动态水印叠加，ShareLink 无法实现 |
| 历史存储 | Documents 目录 + JPEG 缩略图 | 无需数据库，系统备份兼容，50 条自动清理 |
| 推荐机制 | 后端内存存储（MVP） | 快速验证，生产环境可替换为 Cloud Firestore |
| Onboarding 页数 | 4 页 | 核心卖点各 1 页 + CTA，不超过用户耐心 |

---

## 问题 & 解决方案（Phase 3）

| 问题 | 原因 | 解决 |
|------|------|------|
| Cloud Run 部署失败 | Compute SA 缺少 Artifact Registry 写权限 | 添加 `roles/artifactregistry.writer` |
| Cloud Build 日志不可见 | `CLOUD_LOGGING_ONLY` 模式 | 通过 `gcloud builds describe` 定位到镜像推送阶段失败 |
| iPhone 16 模拟器不存在 | Xcode 26 使用 iPhone 17 系列 | 改用 `iPhone 17 Pro` |

---

## 新增文件清单（Phase 3）

```
ios/AIIDPhoto/
├── Views/OnboardingView.swift          # 首次引导（4 页）
├── Views/HistoryView.swift             # 生成历史画廊
├── Managers/AnalyticsManager.swift     # 轻量级事件追踪
├── Managers/HistoryManager.swift       # 历史记录管理
├── Managers/ReferralManager.swift      # 推荐邀请管理
├── Models/GenerationRecord.swift       # 历史数据模型

backend/
├── src/routes/referral.ts              # 推荐 API（register + redeem）
├── public/legal/                       # 法律文档静态文件（从 docs/legal/ 复制）
│   ├── index.html
│   ├── privacy/{en,ja,zh,ko,vi,id,pt}.html
│   └── terms/{en,ja,zh,ko,vi,id,pt}.html
```

## 修改文件清单（Phase 3）

| 文件 | 修改内容 |
|------|---------|
| `AIIDPhotoApp.swift` | +HistoryManager, +ReferralManager, +Onboarding fullScreenCover, +Analytics |
| `ContentView.swift` | +StoreKit 评分引导, +分享按钮/水印, +历史按钮, +Analytics 埋点, +推荐次数消耗 |
| `SettingsView.swift` | 法律 URL 修复 + 邀请好友区块（邀请码/兑换/奖励） |
| `SubscriptionSheetView.swift` | 法律 URL 修复（LegalURLs 枚举） |
| `backend/src/index.ts` | +express.static + referral 路由注册 |
| `backend/Dockerfile` | +COPY public/ ./public/ |

---

## 下一步

1. **ContentViewModel 抽取**：ContentView 已膨胀（1000+ 行），需拆分到 ViewModel
2. **图片裁剪**：上传后交互式裁剪人脸区域
3. **App Store 截图**：7 语言 × 多设备截图自动化
4. **推荐系统持久化**：从内存存储迁移到 Cloud Firestore
5. **Analytics 后端同步**：实现 flush 端点，仪表板可视化
6. **A/B 测试**：Onboarding 流程、付费墙转化率测试
