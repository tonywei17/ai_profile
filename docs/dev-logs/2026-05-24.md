# 开发日志 — 2026-05-24

## 摘要

核对 CN 版线上后端真实状态，修正文档从 GCP/Gemini 旧架构切换到阿里云 ECS + HivisionIDPhotos + 阿里云百炼，中文化 iOS 权限弹窗文案，并完成阿里云正式域名 DNS/HTTPS 配置。

## 阿里云服务器核对与配置

检查与配置结果：

| 项目 | 结果 |
|------|------|
| ECS | 阿里云广州，Ubuntu 22.04 |
| 部署目录 | `/opt/aiidphoto-backend` |
| PM2 进程 | `aiidphoto-backend`，online，运行 `dist/index.js` |
| Node.js | 20.19.6 |
| 本机端口 | `127.0.0.1:9528` |
| Nginx | `aiphoto-cn.foyli.cloud` → `http://127.0.0.1:9528` |
| 本机健康检查 | `http://127.0.0.1:9528/health` 正常 |
| Nginx Host header 健康检查 | 正常 |
| 公网 DNS | `aiphoto-cn.foyli.cloud` A 记录已解析到 ECS |
| HTTPS | Let's Encrypt 证书已配置，公网 `/health` 正常 |
| 后端部署 | 本地最新 `backend/src` 已同步到服务器，服务器端构建通过，PM2 已重启 |
| App Key | `REQUIRE_APP_KEY=true`，无 Key 生成请求返回 401 |
| 法务网页 | 部署在 `https://aiphoto-cn.foyli.cloud/legal/`，含隐私政策、服务条款、支持页、个人信息清单、第三方服务清单 |

## 关键结论

- 阿里云后端确实存在并在线。
- 正式域名已完成 DNS 和 HTTPS，iOS `BACKEND_BASE_URL` 已切到 `https://aiphoto-cn.foyli.cloud`。
- 当前阿里云部署代码已对齐本地最新生成链路。
- `REQUIRE_APP_KEY` 已启用，仍建议上线后继续补账号/设备级滥用保护。

## 文件变更

| 文件 | 变更 |
|------|------|
| `README.md` | 更新为 CN 版说明 |
| `docs/PROJECT-OVERVIEW.md` | 改为阿里云/Hivision/百炼架构与当前状态 |
| `docs/03-architecture/architecture.md` | 重写整体架构和技术债 |
| `docs/07-deployment/cloud-run-deploy.md` | 改为阿里云 ECS 部署指南 |
| `docs/05-security/api-key-management.md` | 改为 CN 后端 Key 管理说明 |
| `docs/04-features/ai-generation/gemini-service.md` | 更新生成链路和生产差异 |
| `project.yml` / `Info.plist` | 权限和跟踪用途说明中文化，后端 URL 切到阿里云域名 |

## 后续建议

- 做 iOS 真机真实图片生成，确认 App Key、Hivision 和百炼链路端到端正常。
- 确认中国大陆 App Store 的 ICP/App 备案号，并补齐 App Store Connect 合规信息。

## 商业模式与内购改版

将 CN 版商业模式从 Pro 订阅/广告切换为一次性消耗型成片制作包：

| 项目 | 结果 |
|------|------|
| StoreKit 商品 | `com.yufeicn.aiidphoto.photo_task_3` |
| 商品类型 | Consumable |
| 权益 | 每包 3 次 AI 生成机会，含高清下载和打印排版 |
| 首发价格 | 本地 StoreKit 配置 `¥3.80` |
| 常规目标价 | `¥9.90/张` |
| 广告/ATT | iOS 工程移除 Google Mobile Ads 依赖、ATT 请求和广告位展示 |

产品规则说明：

- 当前 `¥3.80` 是首发/限时全员优惠，不是可靠的“新用户首单优惠”。
- 如果后续要做真正首单价，需要在 App Store Connect 创建首单价与常规价两个消耗型商品，并通过后端/交易历史判断购买资格。
- 本轮已将首页、购买页和引导页中的“新用户”“首次免费”旧文案清理为“限时优惠”或制作包文案。

## iOS 首页、购买页与设置页 UI 统一

按首页新视觉统一购买页和设置页：

| 页面 | 变更 |
|------|------|
| 首页 | 接入 AI 生成肖像素材，替换占位符；去掉“查看更多”；底部 CTA 增加安全留白 |
| 顶部横幅 | 使用中国年轻女性职业形象图，叠加轻量“AI生成”说明 |
| 真实效果展示 | 使用男生/女生证件照前后对比图，叠加轻量“AI生成”说明 |
| 购买页 | 改为蓝色横幅、权益卡片、固定底部购买 CTA，明确非订阅不自动续费 |
| 设置页 | 从系统 `List` 改为蓝色 Hero + 卡片式设置区，统一制作包、外观、语言、邀请、服务、法务入口 |
| 无障碍 | 首页顶部历史、设置、购买制作包按钮补充中文/多语言 accessibility label |

新增素材来源：

- `backend/public/images/image.png`：首页顶部职业女性横幅。
- `backend/public/images/image1.png`：男生证件照前后对比。
- `backend/public/images/image2.png`：女生证件照前后对比。
- iOS 中同步为 `HomeHeroPortrait`、`ShowcaseMaleComparison`、`ShowcaseFemaleComparison` 三个 asset catalog 资源。

## App Store 合规与物料

补充中国大陆 App Store 上线相关材料：

- 新增法务网页目录，包括隐私政策、服务条款、支持页、个人信息清单、第三方服务清单、数据删除说明和订阅/购买说明页。
- 新增 `docs/13-appstore-assets` 宣传截图生成方案，覆盖 iPhone 与 iPad 首屏宣传图。
- 新增 `docs/18-qa-reports` 两份阶段性 QA 报告，记录当前仍为 `BLOCKED` 的原因：内购审核截图、Sandbox 端到端、ICP/App 备案信息和正式宣传截图。
- 新增 `docs/07-deployment/release-readiness-2026-05-24.md`，汇总阿里云、iOS、法务、内购和上线阻断项。

## 验证记录

本轮使用 `xcodebuildmcp` 完成模拟器验证：

| 检查 | 结果 |
|------|------|
| iOS Simulator build | 通过 |
| iOS Simulator build-and-run | 通过 |
| 首页截图检查 | 顶部职业照、两张对比图、“AI生成”标注、底部 CTA 留白均已显示 |
| 设置页截图检查 | 新版蓝色 Hero + 卡片布局显示正常 |
| 购买页截图检查 | 新版权益页显示正常，无旧订阅页样式 |

## QA 修复补充

根据 `docs/18-qa-reports/20260524_qa.md` 继续修复：

| 项目 | 结果 |
|------|------|
| 客户端 App Key | 从 `project.yml` / `Info.plist` 明文改为 `$(AIIDPHOTO_APP_API_KEY)` build setting；新增 tracked `App.xcconfig` + gitignored `Secrets.xcconfig` 方案 |
| 推荐码 HMAC | 从复用 `APP_API_KEY` 改为独立 `REFERRAL_HMAC_SECRET` |
| 阿里云安全方案 | 文档更新为 KMS Secrets Manager + ECS RAM Role 最小权限读取 |
| Restore Purchases | 设置页新增“恢复购买”入口，调用 `subscription.restore()` |
| 死代码 | 删除 `UsageManager` / `AdManager` / `EditorialToggle` 及注入 |
| backend CVE | `npm audit fix` 后 `npm audit --omit=dev` 为 0 vulnerabilities |
| iOS warning | Release 模拟器构建 0 warnings |
| 价格文案 | UI 和宣传物料统一为 `¥3.80` / `¥9.90` |

修复后验证：

- `cd backend && npm run build`：通过。
- `cd backend && npm audit --omit=dev`：0 vulnerabilities。
- `xcodebuildmcp simulator build --configuration Release`：通过，0 warnings。
- `xcodebuildmcp simulator build-and-run --configuration Debug`：通过；设置页可见 Restore Purchases 入口，购买页可见 `¥3.80`。
