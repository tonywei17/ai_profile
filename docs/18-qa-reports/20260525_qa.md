# 2026-05-25 QA 报告

状态：CONDITIONAL PASS（代码层全部 PASS；外部材料类仍 BLOCKED）
版本：AIIDPhoto 1.1.1 (build 22) / backend 工作区未提交
分支：cn（HEAD 仍为 `1fb7580`，本轮修复未 commit）
审计类型：对 Codex 2026-05-24 修复声明的独立复查

## 1. 摘要

针对 [20260524_qa.md](./20260524_qa.md) 列出的所有 P0/P1/P2 项进行二次验证。Codex 在工作区完成了 6 项 P0/P1 修复与全部 P2 deprecation 清零；复查结果与其声明一致，未发现回归或漏修。剩余阻断完全来自 App Store Connect 与备案系统侧，与代码无关。

| 类别 | 数量 | 状态 |
|------|------|------|
| 代码层 P0 修复（P0-1、P0-2） | 2 | 全部 PASS |
| 代码层 P1 修复（P1-1/2/3/4/6） | 5 | 全部 PASS |
| 代码层 P2 修复 | 全部 | 全部 PASS（dead code / deprecation / 价格文案统一） |
| 外部材料类 P0（P0-3/4/5/6） | 4 | 仍 BLOCKED，需 ASC 与备案系统操作 |
| StoreKit 真机价格验证（P1-5） | 1 | PARTIAL，需中国区真机 Sandbox |

## 2. 复查结果（按 ID 对照）

### P0-1 密钥移出仓库 — PASS

| 验证点 | 证据 |
|--------|------|
| `project.yml:91` 已改为 `APP_API_KEY: "$(AIIDPHOTO_APP_API_KEY)"` | 文件内容确认 |
| `ios/AIIDPhoto/Info.plist:6` 已改为 `$(AIIDPHOTO_APP_API_KEY)` 占位 | 文件内容确认 |
| `project.yml:29-31` 新增 `configFiles: Debug/Release → App.xcconfig` | XcodeGen 渲染机制接通 |
| `ios/AIIDPhoto/Configuration/App.xcconfig`（tracked）含 `#include? "Secrets.xcconfig"` | 软引用机制健全，无 Secrets.xcconfig 时不报错 |
| `ios/AIIDPhoto/Configuration/Secrets.xcconfig.example`（tracked）提供占位模板 | 协作者可拷贝填写 |
| `.gitignore:8-9` 排除 `Secrets.xcconfig` 与 `*.local.xcconfig` | 防止误提交 |
| HMAC salt 解耦：`backend/src/config.ts:15` 新增 `referralHmacSecret`，`backend/src/routes/referral.ts:150` 改用该 secret | 推荐码签名不再依赖 `APP_API_KEY` |
| `backend/.env.example:23` 增加 `REFERRAL_HMAC_SECRET=` | 部署模板同步 |
| `docs/05-security/api-key-management.md` 已重写为阿里云 KMS + ECS RAM Role + xcconfig 注入方案 | 文档与代码一致 |
| 旧 key 历史记录处理 | 仓库为私有仓库，开发者判断不强制轮转；接受，仅在仓库外泄 / 转公开 / 协作者权限变更 / 异常调用时轮换 |

### P0-2 Restore Purchases 入口 — PASS

| 验证点 | 证据 |
|--------|------|
| `ios/AIIDPhoto/Views/SettingsView.swift:213-240` 制作包区新增按钮 | `Task { await subscription.restore() }` 调用到位 |
| 按钮含 `isRestoring` ProgressView 状态 | UX 反馈完整 |
| 同步成功弹 `restoreCompleteTitle` alert，失败走 `subscription.purchaseError` | 边界处理齐全 |
| `restoreLabel` 文案覆盖 7 语言 | SettingsView.swift:617 |
| Apple Guideline 3.1.1 IAP 恢复入口要求满足 | 不再因此被 reject |

### P1-1 / P1-2 死代码删除 — PASS

| 验证点 | 证据 |
|--------|------|
| `ios/AIIDPhoto/Managers/UsageManager.swift` 已删除 | `ls` 不在列表 |
| `ios/AIIDPhoto/Managers/AdManager.swift` 已删除 | 同上 |
| `ios/AIIDPhoto/Views/Components/EditorialToggle.swift` 已删除（附加清理） | 同上 |
| App / ContentView / PhotoCreationView / SettingsView 不再含 `usage` / `adManager` 注入 | grep 仅匹配剩余 4 个合法 manager |
| 工程全量 grep `UsageManager`/`AdManager`/`EditorialToggle` 返回空 | 无残留引用 |

### P1-3 法务链接多语言映射 — PASS

| 验证点 | 证据 |
|--------|------|
| `SubscriptionSheetView.swift:402-410` `legalLang(_:)` 按 `hasPrefix` 真实映射 zh/ja/ko/vi/id/pt/en | 不再硬编码 zh |
| `SettingsView.swift:727` 同步修改为相同映射 | 两处一致 |
| backend `public/legal/privacy/` 与 `terms/` 下 7 语言文件齐全 | 链接可达 |
| 备注：`legalLang("zh-Hans")` 经 `hasPrefix("zh")` 命中 `zh.html`（CN 简体），非 `zh-Hans.html` | 行为正确；`zh-Hans.html` 冗余但不致命 |

### P1-4 backend 依赖 audit — PASS

| 验证点 | 证据 |
|--------|------|
| `npm audit --omit=dev` 输出 `found 0 vulnerabilities` | 现场执行确认 |
| backend `npm run build` 通过 | `tsc` 无报错 |

### P1-5 StoreKit 价格文案统一 — PARTIAL

| 验证点 | 证据 |
|--------|------|
| 全工程 `¥3.8`（无尾零）已清零；统一为 `¥3.80` / `3.80元` / `¥9.90` / `9.90元` | grep 验证 |
| SubscriptionSheetView / ContentView / PhotoCreationView 三处文案一致 | grep 12 处全部为正确格式 |
| 中国区真机 Sandbox 价格确认 | 仍未做，开发同事确认本地 `$0.99` 是模拟器 storefront 问题，需真机闭环 |

### P1-6 结果页底部栏 — PASS

| 验证点 | 证据 |
|--------|------|
| `PhotoCreationView.swift:848-862` 分享按钮已移到 `resultView` 内容区 | 紧邻打印排版按钮 |
| `PhotoCreationView.swift:869-909` `generateBarView` 用 `@ViewBuilder` 包裹 `if currentStep <= 2` | 结果页（步骤 4）底部栏不再渲染 |
| 不再遮挡打印排版入口 | UI 结构上消除冲突 |

### P2 技术债 — 全部 PASS

| 验证点 | 证据 |
|--------|------|
| `ContentView.swift` 中 `topToolbar` / `processingSection` / `outputSection` / `addWatermark` | 全工程 grep 返回空 |
| `UIScreen.main` 使用 | 全工程 grep 返回空 |
| 旧 `onChange(of:perform:)` 闭包旧签名 | 正则 `\.onChange\(of:\s*\w+\)\s*\{\s*\w+\s+in` 全工程返回空 |
| `isSubscribed: true` 硬编码与 `¥3.8`（无尾零） | grep 返回空 |
| Release 模拟器构建 | Codex 工作区记录 0 warning（`build_sim_2026-05-24T14-53-39-328Z`） |

## 3. 现场补充检查

| 检查 | 结果 | 命令 / 证据 |
|------|------|-------------|
| backend `npm run build` | PASS | `tsc` 无输出 |
| backend `npm audit --omit=dev` | PASS | `found 0 vulnerabilities` |
| `plutil -lint ios/AIIDPhoto/Info.plist` | PASS | `OK` |
| `jq empty ios/AIIDPhoto/Configuration/Products.storekit` | PASS | 无错误 |
| backend `config.referralHmacSecret` 接通 | PASS | `config.ts:15` → `referral.ts:150` 链路完整 |
| `.gitignore` 含 `Secrets.xcconfig` 与 `*.local.xcconfig` | PASS | 第 8-9 行 |
| 工程 4 个保留 manager 注入正确 | PASS | subscription / langManager / historyManager / referralManager 链路完整 |

## 4. 仍开放的阻断（与代码无关）

| ID | 类别 | 阻断内容 | 责任方 |
|----|------|----------|--------|
| P0-3 | App Store Connect | 1.0 版本页面文案仍含旧 Pro / 广告 / 订阅 / 每日免费次数 | ASC 操作 |
| P0-4 | App Store Connect | 内购审核截图缺失，`com.yufeicn.aiidphoto.photo_task_3` 未关联到 app 版本 | ASC 操作 |
| P0-5 | 法务 | ICP / App 备案号待确认 | 备案系统 |
| P0-6 | 端到端 | 真机 Sandbox 购买回归未做 | 真机测试 |
| P1-5 | StoreKit | 中国区真机价格确认 | 真机 + 中国区 Sandbox 账号 |

## 5. 残留技术债（不阻断本次上线）

| 位置 | 问题 | 备注 |
|------|------|------|
| `backend/src/routes/tripo.ts` + `backend/src/index.ts:57` | Tripo 3D 路由仍挂载但 iOS 无引用，攻击面无故扩大 | 本轮未处理 |
| `backend/src/routes/gemini.ts:21` | `dailyBudget` 内存计数器 PM2 重启归零 | 本轮未处理 |
| `backend/src/routes/referral.ts:11-12` | `referralCodes` / `redeemedDevices` 内存 Map，PM2 重启全丢 | 本轮未处理 |
| `backend/public/legal/privacy/zh-Hans.html` | 冗余文件，`legalLang` 不会路由到此 | 可后续清理 |
| `project.yml:5-6, 28, 39` | `deploymentTarget: 26.0` 与 CLAUDE.md "iOS 16+" 仍有冲突 | 产品决策，未处理 |

## 6. 复查结论

代码层 BLOCKED 项全部清零。当前不能上线的原因完全在 App Store Connect 元数据、内购审核截图、ICP 备案、真机 Sandbox 回归四类外部材料，与代码质量无关。

建议下一步动作：

1. 立即提交本次工作区修改为新 commit（当前 33 个文件 unstaged）
2. 把今天工作区构建产物（`build_sim_2026-05-24T14-53-39-328Z`）的日志归档到 `docs/18-qa-reports/artifacts/` 或 release-readiness 引用
3. 启动真机 Sandbox 回归 + ASC 元数据清理 + 备案号填写三条并行线，全部完成即可推 1.1.2 提审

## 7. 关联文档

- [20260524_qa.md](./20260524_qa.md)（昨日审计 + Codex 修复声明）
- `docs/07-deployment/release-readiness-2026-05-24.md`
- `docs/05-security/api-key-management.md`（已重写为阿里云 KMS + xcconfig 方案）
- `docs/CHANGELOG.md`（`[1.1.2-unreleased]`）
