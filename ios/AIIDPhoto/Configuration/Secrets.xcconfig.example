// Copy to Secrets.xcconfig for local Xcode builds.
// Do not commit Secrets.xcconfig.
//
// Production values should be supplied by CI or local release settings.
// Client keys are not a strong security boundary; server-side quota and
// App Store transaction validation remain authoritative.

AIIDPHOTO_APP_API_KEY = replace_with_client_calling_key
