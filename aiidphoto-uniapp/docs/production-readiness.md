# 微信小程序上线清单

## 已完成

- 正式构建使用 AppID `wx536140235822460a`。
- 后端与公网健康检查通过：`https://aiphoto-cn.foyli.cloud/health`。
- 登录、生成次数查询、普通支付入账、权益刷新与生成扣次链路已验证。
- 虚拟支付生产配置已部署，线上商品接口返回 3 次生成机会、价格 380 分。
- 正式构建启用合法域名校验，且不包含普通微信支付调用、测试价格或支付密钥。
- 隐私授权弹窗、隐私政策、服务条款已接入。
- 历史记录点击可直接预览成片，不再保留未实现的详情入口。
- 商品文案已统一为“3.80 元购买 3 次生成机会”，已移除原价、限时和用户规模等不可验证宣传。
- 设置页已隐藏未开放的邀请码功能，并提供购买问题联系邮箱。
- “恢复购买/订阅”相关展示已调整为一次性购买和刷新购买状态。
- 已移除未完整适配全页面的深浅色切换入口，正式版本固定使用浅色主题。
- 隐私政策与用户协议页面标题已改为实际名称，不再显示翻译键名。
- 首页效果示例已恢复为 iOS 项目中的男女前后对比图，并明确标注为 AI 生成示例及效果差异说明。
- 微信小程序正式构建已启用组件按需注入：`lazyCodeLoading: requiredComponents`。
- 正式包全部 JSON 配置可解析，基础库版本为 `3.16.1`，满足虚拟支付最低版本要求。
- 小程序备案已完成。
- 结果页打印排版已接入真实图片生成与相册保存，不再包含未完成功能提示。
- 已移除未使用的任意网页 `web-view` 页面和生产包模拟支付接口。
- AI 生成/编辑结果已在界面显式标注，JPEG/PNG 写入国标 XMP 隐式标识；无显式水印导出需用户主动确认并记录审计日志。
- 已补充支付中断恢复：用户付款后即使关闭小程序，再次查询权益也会自动核对并补发待处理虚拟支付订单。
- 公网隐私政策、服务条款、数据删除、支持和第三方清单已与微信小程序登录、虚拟支付、Google Cloud 跨境处理及 AI 内容标识保持一致。
- 审核构建目录：`dist/build/mp-weixin`。

## 已在微信公众平台完成

- `https://aiphoto-cn.foyli.cloud` 已配置为 request 合法域名。
- 主体名称为“羽飞科技(广西)有限责任公司”。
- 服务类目已选择“工具 > 图片处理”。
- 虚拟支付道具已发布现网：3 次 AI 生成机会，价格 3.80 元。
- 小程序备案已完成。

## 上传审核前仍需完成

- 在《用户隐私保护指引》中声明照片选择、相机、保存到相册、OpenID 对应标识、订单信息、IP/请求日志和人像面部信息，填写内容见 `docs/wechat-admin-privacy-guide.md`。
- 在《用户隐私保护指引》中增加 AI 生成内容标识及无显式水印导出日志，并明确敏感人像的必要性与跨境处理。
- 在第三方信息共享清单中声明微信小程序基础能力、微信小程序虚拟支付、阿里云 ECS、阿里云百炼、HivisionIDPhotos 和 Google Cloud，填写内容见 `docs/wechat-admin-privacy-guide.md`。
- 分别使用 iPhone 与安卓或鸿蒙微信客户端完成一次 3.80 元现网支付验收，确认支付成功后生成次数增加 3 次；该步骤会真实扣款，需人工确认后执行。
- 上传前在微信开发者工具执行“代码质量”与“体验评分”检查，并处理阻断项。

## 支付说明

- 正式构建使用 `wx.requestVirtualPayment` 道具直购，商品为 3 次 AI 生成机会，价格 3.80 元。
- 已开通苹果 IAP 支付时，iOS、安卓、鸿蒙和 Windows 均通过微信虚拟支付购买生成次数；客户端按 `wx.requestVirtualPayment` 能力检测决定是否展示购买入口。
- 普通 `wx.requestPayment` 仅保留在独立开发测试模块，不进入正式构建。
- 虚拟支付 AppKey、OfferID 和 ProductID 均只保存在服务端，正式小程序包不包含这些配置。

## 构建与上传

```powershell
cd D:\liu\Desktop\yufeicode\ai_profile\aiidphoto-uniapp
npm run build:mp-weixin
```

微信开发者工具只导入 `dist/build/mp-weixin`，不要导入仓库内旧的示例工程目录。
